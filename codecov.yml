coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 5%
  ignore:
    - "**/*_test.go"
comment:
  layout: "reach, diff, files"
  behavior: default
  require_changes: false
